Append Log
==========

Append Log is used to record key add, update and removal of a zarr :py:class:`Group` (more precisely :py:class:`SquirrelGroup`), and save the logs inside a folder :code:`.squirrel` under the root zarr group.

.. autoclass:: squirrel.caching.append_log.AppendLog
    :members:
