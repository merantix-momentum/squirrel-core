SquirrelGroup
=============
It is advised to use :py:class:`~squirrel.zarr.group.SquirrelGroup` over :py:class:`zarr.hierarchy.Group`, especially
when working with large datasets since the former provides performance boosts over the latter.

squirrel.zarr.group
-------------------
.. automodule:: squirrel.zarr.group
    :members: